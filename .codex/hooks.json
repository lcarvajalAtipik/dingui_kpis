{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); proj=\"${CLAUDE_PROJECT_DIR:-$(pwd)}\"; enc=$(echo \"$proj\" | tr '/_' '-'); case \"$f\" in \"$HOME/.claude/projects/$enc/memory/\"*.md) cp \"$f\" \"$proj/docs/claude-memory/\" ;; esac"
          }
        ]
      }
    ]
  }
}
